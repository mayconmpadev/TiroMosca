<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TiroMosca" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TiroMosca" parent="Base.Theme.TiroMosca" />

    <style name="button" >

        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/bg_redondo</item>
        <item name="textAllCaps">false</item>
        <item name="android:textStyle">bold</item>

    </style>

    <style name="button_apertado" parent="@style/Theme.TiroMosca">

        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/bg_redondo</item>
        <item name="android:backgroundTint">@color/cinza</item>
        <item name="textAllCaps">false</item>
        <item name="android:textStyle">bold</item>

    </style>

    <style name="Button" parent="@style/Theme.TiroMosca">

        <item name="android:textColor">@color/branco</item>
        <item name="android:background">@drawable/bg_btn</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="textAllCaps">false</item>
        <item name="android:textStyle">bold</item>

    </style>

    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/bg_dialog</item>
        <item name="android:windowMinWidthMinor">80%</item>
    </style>
</resources>